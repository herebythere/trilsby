# Trilsby Server

Monolith server for trilsby dot com.

Direct interactions between DB and the client.

This server provides:
- web client interface
- html partials
- json api

branch responses based on type

if url / 
	then if content type?

// json doesn't always output 1:1 to page but it kinda does?

or if content-type
	then url?